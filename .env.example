# Used by docker-compose.yml to register the Gitea Actions runner.
# Get this from: Repo Settings -> Actions -> Runners -> Create new runner.
# Rotate it if it's ever exposed (e.g. committed to git history).
GITEA_RUNNER_REGISTRATION_TOKEN=
